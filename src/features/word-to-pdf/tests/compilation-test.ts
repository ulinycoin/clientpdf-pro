// Простой тест компиляции для проверки исправления
import { PDFGenerator } from '../services/pdfGenerator';

// Создаем экземпляр для проверки компиляции
const generator = new PDFGenerator();

console.log('PDFGenerator compiled successfully!');
